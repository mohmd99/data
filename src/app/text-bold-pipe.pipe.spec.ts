import { TextBoldPipePipe } from './text-bold-pipe.pipe';

describe('TextBoldPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TextBoldPipePipe();
    expect(pipe).toBeTruthy();
  });
});
